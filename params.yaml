project:
  random_seed: 42

mlflow:
  tracking_uri: sqlite:///mlflow.db
  experiment_name: product-recommender

evaluation:
  top_k: 10
  candidate_size: 100
  max_users: 10000

neural:
  embedding_dim: 8
  hidden_dim: 16
  learning_rate: 0.001
  epochs: 2
  batch_size: 8192
  sample_size: 200000
